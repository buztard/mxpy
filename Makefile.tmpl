QUIET_GEN = @echo '   ' GEN $@;

PLATFORM_VERSION = 1.0

# python scripts & extension modules
pkgpythondir = $(pyexecdir)
pkgpyexecdir = $(pyexecdir)

mxpy_common_includes = $(PYTHON_INCLUDES) $(PYGTK_CFLAGS) -I$(top_builddir)/mx
mxpy_common_ldflags = -module -avoid-version
mxpy_defsdir = $(pkgdatadir)/$(PLATFORM_VERSION)/defs
